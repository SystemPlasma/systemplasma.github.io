// Entry point for Vite (non-React)
// Update this file with your app bootstrap logic.

const root = document.getElementById('root');
if (root) {
  // Placeholder content so the app renders something during setup.
  root.textContent = '';
}

export {};

